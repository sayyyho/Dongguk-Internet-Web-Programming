// 반환값 false 넣으면 default 행동 못함
// 매개변수.preventDefault() -> 이벤트의 디폴트 행동 강제취소
// setTimeout -> 정해진 시간에 1번 수행
// interval -> 정해진 시간마다 수행
// script 밖이면 전역변수
const h1 = document.querySelector("h1");
console.log(h1.innerText);

